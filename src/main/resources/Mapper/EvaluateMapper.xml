<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Mapper.EvaluateMapper">

  <resultMap type="evaluate" id="evaluateMap">
  	<id column="evaluateID" property="evaluateID"/>
  	<result column="customerID" property="customerID"/>
  	<result column="goodsID" property="goodsID"/>
  	<result column="evaluateDate" property="evaluateDate"/>
  	<result column="evaluateGrade" property="evaluateGrade"/>
  	<result column="evaluateContent" property="evaluateContent"/>
  	<result column="evaluatePictureID" property="evaluatePictureID"/>
  </resultMap>

  <select id="findByevaluateID" resultMap="evaluateMap">
    select * from Evaluate where evaluateID = #{evaluateID}
  </select>
  
  <select id="findEvaluateByGoodsID" resultMap="evaluateMap">
    select * from Evaluate where goodsID = #{goodsID}
  </select>
  
  
  <insert id="insert">
  	insert into Evaluate values(#{evaluateID}, #{customerID}, #{goodsID}, #{evaluateDate}, #{evaluateGrade}, #{evaluateContent}, #{evaluatePictureID})
  </insert>
  <update id="update">
    update Evaluate set customerID=#{customerID}, goodsID=#{goodsID},evaluateDate=#{evaluateDate},evaluateGrade=#{evaluateGrade},evaluateContent=#{evaluateContent},evaluatePictureID=#{evaluatePictureID} where evaluateID=#{evaluateID} 
  </update>
  <delete id="delete">
    delete from Evaluate where evaluateID=#{evaluateID}
  </delete>
</mapper>